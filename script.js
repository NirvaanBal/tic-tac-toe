const Gameboard = (() => {
  let gameBoard = ['', '', '', '', '', '', '', '', ''];

  return { gameBoard };
})();

const Players = () => {
  let p1, p2;

  return { p1, p2 };
};

const Game = (() => {})();
